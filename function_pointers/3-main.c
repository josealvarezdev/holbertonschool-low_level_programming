#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void main(int argc, char *argv[])
{
int res;
	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}
	if ((argv[3]) == 0 && (argv [2]) % == 0)
	{
		printf("Error\n");
		exit(100);
	}
	if ((*(argv[2] == '/' && (argv[2] == '%')) && (atoi(argv[3])== 0)))
	{
		printf("Error\n");
		exit(100);
	}
	else
	{
	printf("Error\n");
	exit(99);
	}
return (res);
}
