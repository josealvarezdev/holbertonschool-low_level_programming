#include "main.h"
/**
 *  * main - Print alphabet with a function :D
 *   *
 *    * Return: Always 0
*/
void print_alphabet(void)
{
	int alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
	_puthcar(alphabet);
	}
	return (0);
}
