#include "main.h"
/**
 * * print_alphabet - write a lowercase alphabet
 * * Return: no exist
 * */

void print_alphabet(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
	_putchar(alphabet);
	}
	_putchar(10);
}
