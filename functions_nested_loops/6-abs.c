#include "main.h"
/**
 *  *  * _abs - return absolute value of a number
 *   *   * @c: - is a variable dou
 *    *    * Return: 1 if c is a letter, lowercase or uppercase
*/
int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}
}
